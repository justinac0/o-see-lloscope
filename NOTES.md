# notes

## todo Features
- autocomplete for commands
- auto detect scope

# small wins

- Even though there is an auto button on the scope, the auto action allows the user to stay at their computer if they hav mobility problems. This also means a VIP can access the scope functions with out having to ask which button is which.
- capture (at least on QUT scopes) is not possible due to the relyance on a touch screen interface. This capture function allows a completely non sighted user to autoscale and take a screen capture of the current signal.
- The capture can then be played back to the user as an auditory signal to provide a primilinary shape of the graph
- A ML model or LLM could then be used to provide a textual description of the graph.
-  the data can then be outputed to the console to be read by a screenreader.
- the user can then view the 



## user story
turn oscilloscope on an connect usb
- Idealy would automatically detect the oscilloscope
